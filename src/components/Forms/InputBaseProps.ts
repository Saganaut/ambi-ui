/** Shared presentation props used by labelled form controls. */
export interface InputBaseProps {
  label?: string;
  infoMessage?: string;
  errorMessage?: string;
  isBordered?: boolean;
  ariaLabel?: string;
}
